apiVersion: v1
kind: Service
metadata:
  name: svc-rollouts
  namespace: dev
  labels:
    name: svc-rollouts
    type: service
spec:
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: rollouts-dev
