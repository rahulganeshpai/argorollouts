apiVersion: v1
kind: Service
metadata:
  name: rollouts-svc-stable
  namespace: dev
  labels:
    name: rollouts-svc-stable
    type: service
spec:
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    name: rollouts-dev
---
apiVersion: v1
kind: Service
metadata:
  name: rollouts-svc-canary
  namespace: dev
  labels:
    name: rollouts-svc-canary
    type: service
spec:
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    name: rollouts-dev
